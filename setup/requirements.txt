# Data Strategy for LLMs - Consolidated Requirements
# This file contains all dependencies needed across all chapters.
#
# We use minimum-version bounds (>=) instead of exact pins so pip can pick
# prebuilt wheels that match your Python version. Exact pins from 2024
# (tiktoken==0.7.0, chromadb==0.5.5) had no wheels for Python 3.13/3.14
# and forced source builds that fail on most reader machines.

# Core dependencies
openai>=1.40
python-dotenv>=1.0
requests>=2.32
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
jupyter

# Vector database and ML dependencies
chromadb>=1.0
tiktoken>=0.12
torch>=2.6
transformers>=4.44
peft>=0.12
ipywidgets

# Web scraping and document processing
beautifulsoup4>=4.12
pypdf>=4.2
wikipedia-api>=0.6
biopython>=1.85
reportlab>=4.2

# Graph and visualization for Chapter 3 notebooks
networkx
matplotlib
